import type { ToolbarEmits, ToolbarProps, ToolbarSlots } from 'primevue/toolbar';

/**
 * Defines valid properties in Toolbar component.
 */
export type Props = ToolbarProps & {};

/**
 * Defines valid slots in Toolbar slots.
 */
export type Emits = ToolbarEmits & {};

/**
 * Defines valid emits in Toolbar component.
 */
export type Slots = ToolbarSlots & {};
